#include "main.h"

/**
* main - Entry point
*
* Return 1 if c is a latter
*int c to print
*Return: 0 Always
*/
int _isalpha(int )
{
if ((c > 'a' && c < 'z') || (c > 'A' && c <= 'Z'))
{
return (1);
}
else
{
retun(0);
}
}
